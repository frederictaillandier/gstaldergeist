name: Hello World

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    environment: PRODUCTION
    steps:
      - run: echo -n "ansible_user  ${{ secrets.ANSIBLE_USER }}" | base64
