name: Hello World

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo -n "ansible_user ${ANSIBLE_HOST_IP:0:2}" | base64
        env:
          ANSIBLE_HOST_IP: ${{ secrets.ANSIBLE_HOST_IP }}
